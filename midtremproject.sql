select * from tabletest2
select * from tabletest
drop table tabletest2

create table tabletest2(
dis varchar(MAX),
sta varchar(MAX),
address varchar(MAX),
das varchar(MAX),
state varchar(MAX),
cha varchar(MAX), 
fee varchar(MAX),
way varchar(MAX), 
ope varchar(MAX),
sty varchar(MAX), 
no varchar(MAX)
)